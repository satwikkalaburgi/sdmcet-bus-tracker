{
  "name": "smart-bus-tracking-system",
  "version": "1.0.0",
  "description": "Smart College Bus Tracking System Monorepo",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "start:server": "npm start --prefix server",
    "start:client": "npm run dev --prefix client -- --host",
    "start:ai": "cd ai-service && python app.py",
    "dev": "npx concurrently \"npm run start:server\" \"npm run start:client\" \"npm run start:ai\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
